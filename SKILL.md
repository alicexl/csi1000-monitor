---
name: csi1000-monitor
description: Use when user mentions 中证1000 / 中证 1000 / csi1000 / IM 期货贴水 / MO 期权 / 贴水策略, asks to 跑一下监控/拉数据/看报告/看估值/看贴水/查信号, or wants to check whether to enter/reduce/switch IM futures positions based on valuation percentile + basis + call enhancement. Triggers on keywords: csi1000, 中证1000, 贴水, 基差, IM当月/下月/当季/下季, MO2607/2608/2609/2612, PE_TTM 分位, 估值分位.
---

# csi1000-monitor

中证 1000 **Roll Yield Monitor**（贴水策略执行与监控工具）。一键拉数据（PE/PB 历史 + IM 期货 + MO 期权）→ 算多区间估值分位 / 展期收益 roll yield / 1σ OTM call 增厚 → 输出 7 种信号 + Markdown 报告。

**定位：策略执行与监控工具，不是回测验证过的量化策略。** 它每天判断"当前是否满足策略假设"、监控假设有没有失效、在假设成立时给出执行信号。

不负责：实盘下单、回测、其他指数（沪深 300 / 中证 500 用对应工具）。

## 项目目录

本仓库根目录（GitHub: alicexl/csi1000-monitor）

## 用法

入口：`python monitor.py <子命令>`

| 子命令 | 作用 |
|---|---|
| `run` | 自动判断：DB ≥ 目标交易日则跳过拉取，否则先拉数据（网络 30-60 秒）；然后生成报告 |
| `status` | 一行快速查当前信号 |

### 默认执行

用户说"跑一下" / "看看" / 没指定子命令 → 跑 `run`：

```bash
# 在仓库根目录执行
python monitor.py run 2>&1 | grep -v "UserWarning\|from pandas"
```

周末/假日 `get_futures_daily` 返回 0 合约是正常的，工作日重试。

## 策略逻辑（解读数据用）

- **入场条件**（三条件全满足）：PE_TTM 10y 分位 <50% **且** PB 10y 分位 <50% **且** roll_yield >0（价格 back）
- **预警入场**：部分条件满足但未全达标（warn_entry 列出 PE/PB/roll 三条件的 ✓/✗ 缺口）
- **开仓信号检查**（空仓报告专属面板）：把入场三条件逐条对照（分位区间标签 + 门槛 + 达标），末行给「✅ 符合开仓信号」/「❌ 未达开仓（N/3 满足）」
- **平仓条件**：PE_TTM 10 年分位 >85% **或** 展期双段<0（当月→下月<0 **且** 当月→下季<0，双段 contango；单段 contango 视为噪音走 hold 不离场）
- **预警平仓**：PE_TTM 分位 75–85%
- **合约切换**：当月剩余天数 <7 天时切下月
- **卖 Call 增厚**：持有 IM 多头时卖 1σ OTM 当月 call 增厚收益（按 ATM IV 算 1σ strike，IV 变化时自动伸缩）

策略本质：低估时入场，吃 backwardation 下的展期收益（roll_yield）+ 估值上涨。价格 back（roll_yield>0）才持有；展期双段 contango（当月→下月+当月→下季均<0）才离场，单段 contango 视为交割噪音走 hold。入场判断"价格底（PE/PB 分位）+ 展期收益（roll_yield）"两轴；BPS 回归仅作附录证明净资产底抬升，不参与入场（BPS 偏离趋势线反映盈利周期而非估值便宜），下行保护改由「1年贴水覆盖 -1σ」量化。

## 验证边界（解读/展示时遵守）

向用户解读时**不要宣称"策略能赚钱/已回测验证"**。区分已验证与假设：

| 内容 | 性质 |
|---|---|
| PE/PB 分位、roll_yield 正负、基差贴水、BS 定价 | ✅ 已验证（客观计算） |
| IM 长期以 backwardation 为主 | ⚠️ 经验假设（IM 2022-07 上市，仅 4 年样本） |
| PE<50% 入场 / PE>85% 平仓阈值 | ⚠️ 经验规则（未优化验证） |
| 双段 contango 才离场 | 🎨 策略设计（有解释） |
| 1σ OTM call 增厚长期为正 | ⚠️ 经验规则（未验证） |
| 策略长期超额收益 | ❌ 尚未验证 |

用户问"策略收益/能赚钱吗"时说明：本项目是**纪律化执行框架**，IM 期货 2022-07 才上市、期限结构样本不足 4 年，无法证明稳定超额收益；它做的是假设成立时给信号、假设失效时提示离场。

## 估值反推：预测入场点位

用户问"指数还要跌多少才能入场"时，用**当前** close/PE/PB 实时反推（别引用旧快照数字）：

```
隐含盈利 E = close / PE_TTM      隐含净资产 B = close / PB
触发 PE_TTM 分位 <X% → close = (历史 PE_TTM X% 分位) × E
触发 PB    分位 <X% → close = (历史 PB    X% 分位) × B
```

PE 看 E、PB 看 B 两口径独立反推，共振即估值底。注意：① PE 分位 ≠ 价格分位（盈利涨跌会改 PE，PE/PB 分位常背离，单看 PE 易误判）；② 历史底 PB<1.8（约 3.6% 分位）；③ 从平仓区（85%）回到入场区（50%）历史约需 1-2 年 + 30% 跌幅。历史分位阈值从 DB 的 PE/PB 序列取（10y 窗口），**不写死数字**。

## 7 种信号

| 持仓状态 | 信号 | 含义 |
|---|---|---|
| 空仓 | `entry` | 三条件达标（PE<50% + PB<50% + roll_yield>0），建议入场 |
| 空仓 | `warn_entry` | 部分条件达标，列三条件缺口 |
| 空仓 | `wait` | 估值偏高或贴水不足 |
| 持仓 | `reduce` | 估值过高，平仓 |
| 持仓 | `warn_reduce` | 接近平仓区 |
| 持仓 | `switch` | 当月临近交割，切下月 |
| 持仓 | `hold` | 继续持有 |

## 展示结果

报告路径：`reports/csi1000_YYYY-MM-DD.md`（仓库根下）

读取后向用户展示：

1. **信号 + 状态**：空仓 🟡 / 持仓 🟢 + 信号名
2. **信号检查面板**（紧跟信号，按持仓状态切换）：
   - 空仓→**开仓信号检查**：入场三条件逐条对照（PE<50% + PB<50% + 展期>0，全满足 AND），末行「✅ 符合开仓信号」/「❌ 未达开仓（N/3 满足）」
   - 持仓→**平仓信号检查**：平仓条件逐条对照（PE>85% 或 展期双段<0=当月→下月<0且当月→下季<0，任一触发即平仓；展期拆月段/季段两行，双段均<0才算失效），PE 标签 安全区(≤75%)/预警区(75-85%)/平仓区(>85%)，末行「⚠️ 触发平仓信号」/「✅ 未触发平仓（继续持有）」
3. **持仓盈亏**（holding 专属，紧跟平仓检查）：入场价 vs 当前价浮盈
4. **估值面板**：PE_TTM / PB 的多区间分位（近10年/5年/全历史）
5. **PB 分位情景点位**：固定 B 看 PB 经验分位(50%/15.9%(-1σ)/1%(-2σ)≈2024 危机支撑区 1.60)情景对应点位与跌幅（估值杀跌风险，σ 概率标签标"出现难易"）
6. **贴水覆盖性**：持有 1 年的展期贴水（按当前下季贴水线性累计）vs PB -1σ/-2σ 跌幅，逐格标 ✅已覆盖/❌未覆盖（贴水只看 1 年：-1σ 主判常态杀跌应覆盖，-2σ 极端参考不要求；3 年才覆盖说明下行保护不足）
7. **期货合约**：IM 当月/下月/当季/下季的基差和年化贴水率
8. **卖 Call 增厚**（持仓专属）：1σ OTM call 的执行价/≈σ/ATM IV、权利金、年化增厚率、行权概率
9. **持仓预期收益**（持仓专属）：持有 IM 多头时长期年化回报拆解（ROE+分红+估值变动）
10. **附录：BPS 净资产趋势回归**（净资产底抬升证明）：BPS=close/pb 对**全量 BPS 点**对数回归（年化增长率%）+ 全量线性回归（每年+N点），展示 R²；附趋势图。**方法论证明，不参与信号判断**

## 配置

阈值在 `signals.py` 的 `Thresholds` dataclass（入场/平仓阈值）+ `monitor.py` 顶部的 `PCT_WINDOWS` / `FIT_MIN_POINTS`（BPS 回归最少点数）。

**持仓状态用 CLI 子命令持久化到 SQLite**：

```bash
python monitor.py open IM2608 7000 2026-07-18   # 开仓
python monitor.py close                          # 平仓
```

所有命令读 DB 决定空仓/持仓分支；报告逻辑会自动切换（空仓看入场条件，持仓看平仓/切换/盈亏）。

## 常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| scan 返回 0 合约 | 周末/假日 | 工作日重试 |
| option 部分缺失 | 周末无实时数据 | 工作日重试 |
| PE/PB 数据空 | 网络问题 | 重试 `python monitor.py run` |
| `KeyError` 列名 | akshare 改了列名 | 检查 `data_fetcher.py` 的 rename 映射 |
| `stock_index_pb_lg` 报 `pb_w` | 第 4 列是等权不是加权 | 自己算分位（详见 README §十二 已知坑）|
